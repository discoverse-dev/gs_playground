# GS Playground 设计哲学

**这是一个以视觉为中心的 Robot Learning 基础设施，将服务成千上万的用户和研究者。**

## 核心原则

- **每一行代码都会被放大**: 糟糕的设计会被无限复制，影响所有下游用户
- **零容忍垃圾代码**: 冗余、不清晰、低效的代码必须重构或删除
- **架构优先**: 底层设计决定上层质量，必须深思熟虑
- **性能至关重要**: 缓慢的执行会浪费无数 GPU 时间和研究者的时间

## 设计要求

1. **清晰性 > 简洁性 > 灵活性**: 代码首先要易懂，其次要简洁，最后才考虑灵活性
2. **结构化 > 功能性**: 好的结构比快速实现功能更重要
3. **删除 > 注释**: 无用代码直接删除，不要注释掉
4. **重构 > 堆砌**: 发现设计问题立即重构，不要在烂代码上堆新功能
5. **测试 > 调试**: 写可测试的代码，而不是依赖调试

## 代码审查标准

每次提交前自问：
- 这段代码是否清晰到新人能立即理解？
- 是否有冗余或可以删除的部分？
- 性能是否已优化到合理水平？
- 接口设计是否足够通用和稳定？
- 是否遵循了现有的架构模式？

## 架构设计原则

### 模块化分层架构
```
env/                    # 后端抽象层 (Backend Abstraction)
├── base.py            # ABEnv 抽象基类
├── motrix_env/        # 3DGS 渲染后端
├── mujoco_env/        # MuJoCo 物理后端
└── mjx_env/           # MJX 并行后端

{task_type}/           # 任务层 (Task Layer)
├── base/              # 任务基类与配置
├── {robot}/           # 机器人特定实现
└── scripts/           # 训练/推理入口
```

### 结构化编程
- 单一职责: 每个类/函数只做一件事
- 清晰命名: 变量/函数名描述其用途
- 避免硬编码: 使用配置类管理参数
- 最小化依赖: 模块间低耦合

### 配置驱动设计
- 所有任务使用 dataclass 配置类
- 配置与实现分离: `{robot}_config.py` + `{robot}.py`
- 继承链: `base config` → `robot config` → `task config`

### 接口统一
- 所有环境继承 `ABEnv` 抽象基类
- 标准接口: `observation_space`, `action_space`, `num_envs`, `cfg`
- 任务扩展钩子: `task_gaussians()`, `_randomize()`

## Git 规范

### 分支管理
- 主分支: `main` (稳定版本)
- 开发分支: `dev/<feature>` (如 dev/airbot, dev/nav_exp)

### Commit 规范
- **频繁 commit**: 每完成一个小功能立即提交
- **禁止 push**: 本地开发禁止 push 到远程 (除非明确需要)
- 提交信息: 简洁英文小写，描述具体改动
  - 好: `update airbot mink`, `fix xml`, `add lidar support`
  - 差: `update`, `fix bug`, `wip`

## 开发环境

- Conda 环境: `gsp` (必须使用)
- Python 3.10+
- RL 框架: rsl_rl v1.0.2 (PPO)
